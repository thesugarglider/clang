#include<stdio.h>
#include<string.h>

void main(){
    char s[1000];

    printf("Enter the string in capital letter only: ");
    gets(s);

    

    printf("length of the string is %ld\n",strlen(s));

}